- Prove the loop: raw output, then the agent reasons over it directly, then
  the agent writes an adapter, then the adapter gets used, then the adapter
  gets cheaper and more accurate over time.

This is the main agent loop raw stdin and stdout output the agent reasons over it directly then 
the agent codes an adapter(how does it run the adapter). then the adapter gets used, then the adapter gets 
better. 
